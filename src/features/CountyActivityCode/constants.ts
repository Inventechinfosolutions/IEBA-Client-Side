/** Backend `GET /activities` rejects `limit` above this (see API validation). */
export const COUNTY_ACTIVITY_ACTIVITIES_API_MAX_LIMIT = 100

/** Delay before committing search text to the county activity list query (toolbar input). */
export const COUNTY_ACTIVITY_SEARCH_DEBOUNCE_MS = 400
